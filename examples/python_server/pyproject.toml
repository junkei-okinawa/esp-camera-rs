[project]
name = "python-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pyserial>=3.5",
    "pyserial-asyncio>=0.6",
]

[dependency-groups]
dev = [
    "ruff>=0.11.5",
]
