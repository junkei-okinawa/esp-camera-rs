[project]
name = "python-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "influxdb-client>=1.48.0",
    "pyserial>=3.5",
    "pyserial-asyncio>=0.6",
]

[dependency-groups]
dev = [
    "ruff>=0.11.5",
]
